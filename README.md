# Statamic 3 Example App

## Installation, for now...

```
git clone git@github.com:statamic/three-statamic.git statamic-app
cd statamic-app
composer install
cp .env.example .env
php artisan key:generate
php please make:user
```
